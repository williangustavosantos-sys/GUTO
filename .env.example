# Backend local (npm run dev no guto-backend)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Backend produção (Render) — use no .env.local ou nas env vars da Vercel
NEXT_PUBLIC_API_URL=https://cerebroguto.onrender.com
